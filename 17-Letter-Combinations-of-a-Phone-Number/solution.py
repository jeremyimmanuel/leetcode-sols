# PENDING

from typing import Dict, List


def letterCombinations(digits: str) -> List[str]:
    phone_dict: Dict = {
        "2": ["a", "b", "c"],
        "3": ["d", "e", "f"],
        "4": "g h i".split(),
        "5": "j k l".split(),
        "6": "m n o".split(),
        "7": "p q r s".split(),
        "8": "t u v".split(),
        "9": "w x y z".split(),
    }

    ans = []
    level = 0
    while level < len(digits):
        return

    return
